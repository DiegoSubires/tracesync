{
  "name": "tracesync-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:hub": "cd tracesync-hub && npm run build",
    "build:chamber": "cd chamberInventoryMP && npm run build",
    "build:all": "npm run build:hub && npm run build:chamber",
    "predeploy": "npm run build:all",
    "deploy": "gh-pages -d tracesync-hub/dist -m \"Despliegue global del ecosistema TraceSync\"",
    "deploy:hub": "npm run build:hub && gh-pages -d tracesync-hub/dist -m \"Deploy Hub\"",
    "deploy:chamber": "npm run build:chamber && gh-pages -d chamberInventoryMP/dist -m \"Deploy Chamber\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tree-node-cli": "^3.0.0"
  }
}
